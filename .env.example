# Set by `npm run db:branch:create` — no need to fill in manually for local dev.
DATABASE_URL="postgresql://..."

# WorkOS AuthKit — grab from https://dashboard.workos.com
WORKOS_CLIENT_ID="client_..."
WORKOS_API_KEY="sk_test_..."
# 32+ characters. Generate with: openssl rand -base64 32
WORKOS_COOKIE_PASSWORD="replace-with-at-least-32-characters"
NEXT_PUBLIC_WORKOS_REDIRECT_URI="http://localhost:3000/auth/callback"

# --- Neon (optional, only for CI / Vercel) ---
# Local dev: run `npx neonctl auth` once, then `npm run db:branch:create`.
# These two are only needed in headless environments (Vercel/CI) where
# browser OAuth isn't possible.
# NEON_API_KEY=""
# NEON_PROJECT_ID=""
