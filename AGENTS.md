# AGENTS.md

## Cursor Cloud specific instructions

This is a freshly initialized repository (MIT License, OpenCompany). As of the initial setup, it contains no application code, dependencies, build configuration, or tests.

- **No services to run.** There are no backend, frontend, or database services.
- **No dependencies to install.** There is no `package.json`, `requirements.txt`, `go.mod`, or equivalent.
- **No lint/test/build commands.** There are no configured linters, test suites, or build steps.

When application code is added to this repository, update this file with:
1. How to install dependencies and which package manager to use.
2. How to run lint, tests, and the dev server.
3. Any non-obvious setup caveats or environment requirements.
