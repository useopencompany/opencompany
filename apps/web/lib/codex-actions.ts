// Rollback adapter: execution ownership lives in the shared package so the runner
// and this legacy internal route invoke the same persisted application service.
export * from "@opencompany/goat-agent/application/persisted-action-gateway";
