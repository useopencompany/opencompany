import PersonalHome from "@/components/personal/PersonalHome";

// Default /personal view: inbox attention cards + hero composer. Auth, agent provisioning, and
// the provider/chrome stack are handled by the personal layout.
export default function PersonalPage() {
  return <PersonalHome />;
}
