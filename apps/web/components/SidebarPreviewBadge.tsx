export function SidebarPreviewBadge() {
  return (
    <span className="shrink-0 rounded-[3px] bg-ink/[0.07] px-1.5 py-0.5 text-[9px] font-semibold uppercase tracking-[0.04em] text-ink-subtle">
      Early preview
    </span>
  );
}
