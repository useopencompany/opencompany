---
title: The .agent file
description: Every agent in opencompany is a single text file. Here's what's in it and how to write one.
---

## The whole thing in one screen

An `.agent` file has two parts: a short header with metadata, and a body of instructions you write in plain English.

```yaml
---
title: "Fundraising copilot"
model: openai/gpt-5.4
tools:
  - exa
brain:
  - /
  - docs/README.md
agents:
  - path: agents/sales-research/sales-research.agent
    name: Sales research
integrations:
  github:
    repositories:
      - id: opencompany-web
        fullName: opencompany/web
        defaultBranch: main
---

Research investors with @exa. Write careful thesis summaries. Read @brain/ first. Ask @agent/sales-research for account notes. Work in @opencompany/web with @amp.
```

That's it. The `---` lines fence off the header. Everything below is what the AI model reads as instructions.

## The header

The header is YAML — a simple `key: value` format. These are the main fields.

### `title`

The name of the agent. Used in the sidebar, in URLs, and as a hint to the model about what its job is.

```yaml
title: "Fundraising copilot"
```

If you leave it empty, opencompany fills in `"Untitled agent"` so the file is always valid.

### `model`

Which AI model runs this agent. Pick one of:

| ID | What it's good for |
| --- | --- |
| `openai/gpt-5.4-mini` | The default. Fast and cheap. Use for most agents. |
| `openai/gpt-5.4-nano` | Lowest-cost GPT for high-volume lightweight tasks. |
| `openai/gpt-5.4` | Slower, smarter. Use when accuracy matters more than speed. |
| `anthropic/claude-haiku-4.5` | Cheap and fast, Claude flavor. |
| `anthropic/claude-sonnet-4.6` | Smart and careful, especially good at code and structured writing. |
| `anthropic/claude-opus-4.7` | Most capable Claude option for demanding work. |
| `anthropic/claude-opus-4.8` | Latest, most capable Claude option for demanding work. |
| `anthropic/claude-fable-5` | Mythos-class Claude for long-running, complex tasks where other models need more check-ins. |
| `google/gemini-3-flash` | Fast, popular Gemini model with a long context window. |
| `google/gemini-3.1-flash-lite-preview` | Very fast and low cost for simple high-volume tasks. |
| `deepseek/deepseek-v4-flash` | High-throughput model for cost-sensitive work. |
| `mistral/mistral-medium-3.5` | Balanced Mistral model for quality, latency, and cost. |
| `minimax/minimax-m3` | Latest MiniMax with 1M context and agentic coding strength. |
| `minimax/minimax-m2.7` | High-capability MiniMax for software engineering agents. |
| `minimax/minimax-m2.7-highspeed` | Fast MiniMax M2.7 variant for latency-sensitive agent work. |
| `minimax/minimax-m2.5` | MiniMax for full-stack and multi-file code work. |
| `minimax/minimax-m2.5-highspeed` | Fast MiniMax M2.5 variant for responsive coding workflows. |
| `minimax/minimax-m2.1` | MiniMax for reliable agentic coding with interleaved thinking. |
| `minimax/minimax-m2.1-lightning` | Speed-optimized MiniMax M2.1 for fast coding assistance. |
| `minimax/minimax-m2` | MiniMax MoE model for coding and agentic tasks. |
| `moonshotai/kimi-k2.6` | Latest Kimi for long-horizon coding and agent workflows. |
| `moonshotai/kimi-k2.5` | Kimi multimodal model for agents, coding, and vision tasks. |
| `moonshotai/kimi-k2-thinking` | Kimi reasoning model for long tool-call chains. |
| `moonshotai/kimi-k2-thinking-turbo` | Faster Kimi reasoning variant for interactive workflows. |
| `moonshotai/kimi-k2-turbo` | Speed-optimized Kimi K2 for latency-sensitive tool use. |
| `moonshotai/kimi-k2` | Kimi K2 instruct model for coding and agentic pipelines. |
| `xai/grok-4.3` | Latest Grok reasoning model with 1M context and tool use. |
| `xai/grok-4.20-reasoning` | Long-context Grok reasoning model for agent workflows. |
| `xai/grok-4.20-non-reasoning` | Long-context Grok model for direct tool-using tasks. |
| `xai/grok-4.1-fast-reasoning` | Fast, low-cost Grok reasoning model with 1M context. |
| `xai/grok-4.1-fast-non-reasoning` | Fast, low-cost Grok model for direct answers. |
| `xai/grok-build-0.1` | xAI coding model for fast agentic software development. |
| `zai/glm-5.1` | Latest GLM for coding-heavy and agentic engineering tasks. |
| `zai/glm-5-turbo` | Faster GLM 5 variant for production agent workflows. |
| `zai/glm-5v-turbo` | Multimodal GLM 5 model for visual coding and GUI tasks. |
| `openrouter/fusion` | OpenRouter multi-model router for research, critique, and high-stakes answers. |

If you type something that isn't on this list, opencompany falls back to `openai/gpt-5.4-mini`.

All models are accessed through Vercel AI Gateway, so you never deal with provider API keys directly.

### `tools`

A list of capabilities the agent can use beyond text generation. A simple tool
can be written as a string. A tool with persisted configuration is written as an
object.

```yaml
tools:
  - exa
  - id: amp
    type: coding_agent
    provider: amp
    prCapable: true
  - id: codex
    type: coding_agent
    provider: codex
    label: Codex
    description: Delegate coding work to Codex inside an E2B sandbox, with attached or public GitHub repository support.
    prCapable: true
```

GitHub repository access is derived from body mentions, not the `amp` tool. A
plain `@github` mention grants live access to any repository the workspace's
GitHub connection can reach and serializes with
`integrations.github.allRepositories: true`. Concrete mentions like `@owner/repo`
or `@github/owner/repo` serialize to `integrations.github.repositories`. Agents
with GitHub access get a dedicated `gh` tool and can clone on demand into
`work/`; with live `@github` access, pass an explicit repository (`repository`
for coding tools, `--repo owner/repo` for `gh` commands).

| ID | What it does |
| --- | --- |
| `exa` | Web research with search, content extraction, people lookup, and cited answers. |
| `x` | Read public X posts, profiles, timelines, discussions, and trends through the official X API. |
| `youtube` | Search YouTube, inspect video/channel metadata, list channel videos, and fetch transcripts through Supadata. |
| `tiktok` | Inspect public TikTok profiles, recent videos, comments, and search results through Apify; fetch direct-video metadata/transcripts through Supadata. |
| `instagram` | Inspect public Instagram profiles, recent posts/reels, comments, and profile search results through Apify; fetch direct-media metadata/transcripts through Supadata. |
| `amp` | Coding work delegated into a sandboxed runtime, operating on an attached repository. |
| `opencode` | Coding agent delegated into a sandboxed runtime, with attached or public GitHub repository support. |
| `codex` | Codex coding agent delegated into a sandboxed runtime, with attached or public GitHub repository support. |
| `linear` | Workspace MCP access to Linear issues, projects, and comments. |
| `slack` | Workspace MCP access to Slack search, messages, files, emoji, and users. |
| `notion` | Workspace MCP access to Notion search, pages, databases, comments, users, and teamspaces. |

Unknown tool names are silently ignored, so adding a tool that doesn't exist yet won't break the file. Tool labels, descriptions, and runtime function names come from the platform catalog, not from this file.

### `brain`

A list of long-lived context files or folders the agent can access in a session.

```yaml
brain:
  - /
  - docs/README.md
  - product/
```

These paths live under `brain/` in the workspace repo. Use `/` to mount the whole Brain root. File paths mount one file. Folder paths end in `/` and mount matching files below that folder.

### `agents`

A list of other agents in the same workspace that this agent can delegate work to.

```yaml
agents:
  - path: agents/sales-research/sales-research.agent
    name: Sales research
```

The body mention is `@agent/<slug>`, where `<slug>` is the agent folder name under `agents/`. During a run, these references expose a delegation tool. The child agent runs in an inspectable child session hidden from sidebar history, and its answer is returned to the parent as a tool result.
The result also includes `childSessionId`; passing that id back as `sessionId` with a new prompt continues the same delegated child session.

### `integrations.github`

GitHub access is derived from body mentions. A plain `@github` mention enables
live workspace-wide access:

```yaml
integrations:
  github:
    repositories: []
    allRepositories: true
```

`allRepositories: true` is not a snapshot of repository names. At runtime,
repository-aware coding tools resolve the requested `owner/repo` against the
workspace's connected GitHub installations. Agents with this access must pass an
explicit repository, such as the `repository` argument for coding tools or
`--repo owner/repo` for `gh` commands.

### `integrations.github.repositories`

A list of workspace-authorized GitHub repositories referenced by the body.
Both `@owner/repo` and `@github/owner/repo` serialize to the same repository
object.

```yaml
integrations:
  github:
    repositories:
      - id: opencompany-web
        fullName: opencompany/web
        defaultBranch: main
        binding:
          provider: github
          resourceType: repository
          externalId: "123456789"
          displayName: opencompany/web
          connection:
            externalId: "987654"
            label: OpenCompany
            accountName: opencompany
            accountType: Organization
```

`fullName` remains the readable reference shown in body mentions. `binding` is optional normalized runtime data that identifies the concrete provider resource and parent connection without making opaque database IDs part of the human-authored reference. Unknown or unauthorized repository mentions stay in the body as text, but they do not contribute to the saved config.

### `triggers`

Triggers describe external or scheduled ways to start an agent. Scheduled runs use generated preset cron expressions, not natural-language parsing:

```yaml
triggers:
  - id: weekday-brief
    type: agent.schedule
    cron: "0 9 * * 1-5"
    timezone: America/Los_Angeles
    prompt: Review open priorities and write a concise status brief.
    enabled: true
```

The editor creates these from **Run every...**. The prompt is submitted as the first user message whenever the schedule fires.

## The body

The body is plain Markdown. Whatever you write here, the model reads as its instructions. Treat it like an SOP for a new hire: describe the outcome, the constraints, and the style.

A good body says:

- **What success looks like.** "Return one paragraph per fund." "Always include a source link."
- **What to avoid.** "Don't include funds with fewer than three investments this year."
- **Who the output is for.** "Write for a founder reading on their phone."

There's no length limit, but shorter is usually better. If the instructions are getting long, that's often a sign the agent should be two agents.

## Mentions

Inside the body you can write `@something` to attach a tool, Brain path, workspace agent, or GitHub repository. opencompany reads these mentions out of the body and uses them to fill in those header fields — so the body is the source of truth for tools and context. The model is explicit per-agent config in the header.

```text
Find investors with @exa.        ← tool: exa
Read @brain/product/ first.      ← Brain folder
Work in @opencompany/web.        ← GitHub repository
Read @brain/ first.              ← Brain root folder
Ask @agent/sales-research.       ← workspace agent
```

This is what mentions can be:

- A tool ID, like `@exa`
- A Brain path, like `@brain/`, `@brain/docs/README.md`, or `@brain/product/`
- A workspace agent, like `@agent/sales-research`
- A GitHub `owner/repo`, like `@opencompany/web`

If you write `@something` that doesn't match anything, opencompany leaves it as plain text. It won't error, but it also won't do anything special.

### Profile

Every session loads one small file from the agent folder straight into the agent's context, so it remembers durable context without looking it up: `agent/user.md` (who the user is — preferences, style, goals). It's created empty, edited by the agent as it learns, and kept small (capped at ~3 KB when loaded). The profile is not a general facts store — every other durable fact (people, companies, projects, decisions, lessons) goes into structured memory via the `memory` tool. The agent's edits sync at the end of a turn and are reloaded on its next turn in the same session — no new session needed.

### After-session memory

Add `#after-session` to run a background memory pass after a session has been idle for 3 minutes:

```text
Help with customer calls. #after-session Update memory with durable preferences and decisions from the transcript.
```

The hook prompt is the text after the first `#after-session` marker through the end of that paragraph. It does not create a visible chat message; it reuses the agent loop and can update the profile (`agent/user.md`) and structured memory (via the `memory` tool) when there is useful long-lived context to save — keeping the profile tight. Use Brain only for shared company knowledge.

### Tiptap content is not the contract

The web editor stores Tiptap JSON so mentions can render as chips when you reopen an agent. That JSON is only a presentation cache. The `.agent` body text is the contract, and mention-backed runtime config is derived from the body text that will be written to GitHub.

If body text and Tiptap JSON ever disagree, the body wins. This keeps stale editor state from removing repository bindings or tool config during a save.

## Saving and syncing

When you save an agent:

1. The change is stored immediately. Refresh, close the tab, do whatever — your edit is safe.
2. A few seconds later, opencompany commits the file to your workspace's GitHub repo.
3. If the commit fails for any reason (rare), you'll see a warning on the agent's row. Your work is still saved; only the GitHub copy is behind.

You can also edit the file directly in GitHub or in your local editor. The next time opencompany reads from the repo, your changes show up.

## Where files live

In your workspace's GitHub repo, agents live under `agents/`:

```text
your-workspace-repo/
├── agents/
│   ├── fundraising-copilot/fundraising-copilot.agent
│   ├── ops-triage/ops-triage.agent
│   └── sales-research/sales-research.agent
└── brain/
    ├── docs/README.md
    └── product/positioning.md
```

The filename comes from the title — lowercased, with spaces turned into dashes. Rename the title, and the next save will use the new filename.

## Examples

### A small one

```yaml
---
title: "Notes"
model: openai/gpt-5.4-mini
tools: []
---

Summarize whatever I paste below in three bullets. Skip preamble.
```

### A research agent

```yaml
---
title: "Investor research"
model: openai/gpt-5.4
tools:
  - exa
---

Find investors active in seed-stage developer tools. For each fund:

- Use @exa to find their most recent investment in the space.
- Write one paragraph covering thesis, typical check size, and that recent
  investment.
- Include a link to one primary source per fund.

Skip funds that haven't made an investment in the last 12 months.
```

### One where mentions do the work

You can write only the instructions and let opencompany figure out the header:

```yaml
---
title: "Research"
model: openai/gpt-5.4-mini
tools:
  - exa
---

Find investors with @exa. Write a concise brief before sending.
```

After saving, the header above is what ends up in the file — even though you only wrote the body. The `@exa` mention added the tool. The model remains explicit per-agent config.

### A coding agent for a repo

```yaml
---
title: "Web code reviewer"
model: openai/gpt-5.4-mini
tools:
  - id: amp
    type: coding_agent
    provider: amp
    prCapable: true
integrations:
  github:
    repositories:
      - id: opencompany-web
        fullName: opencompany/web
        defaultBranch: main
---

Review pull request context in @opencompany/web and use @amp for implementation work.
```

## How forgiving the format is

You don't need to be careful with this file. The parser is built to never reject what you wrote.

- Missing header? Treated as a body with default settings.
- Unknown model name? Falls back to the default.
- Unknown tool name? Dropped.
- Weird whitespace or line endings? Cleaned up automatically.

The goal is that you can write what's in your head, save, and trust that the file is valid.

## Questions

**Can I write `.agent` files by hand and commit them?**
Yes. Push them to your workspace's GitHub repo, then trigger an import from the app. opencompany reads the format the same way no matter who wrote it.

**Can two people edit the same agent at the same time?**
Yes, but the last save wins for now. We're working on something better. In the meantime, if you're collaborating on a complex agent, it's worth coordinating.

**How do I add a new model or tool?**
That requires a code change — we ship a curated catalog so every workspace knows the same set of models and tools work. Open a request and we'll prioritize.

Tool availability is static for now: every workspace can use every supported catalog tool. The runtime catalog records each tool's default enabled state, platform env requirements, and required workspace resource type when applicable, so future admin controls can use catalog defaults without changing `.agent` files.
