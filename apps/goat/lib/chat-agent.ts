// Shim: the chat agent core moved to @opencompany/goat-agent so apps/runner can
// use it too. Keep importing from "@/lib/chat-agent" inside the app.
export * from "@opencompany/goat-agent/chat-agent";
