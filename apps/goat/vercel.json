{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "bun install --frozen-lockfile",
  "buildCommand": "bun run vercel-build",
  "regions": ["fra1"],
  "crons": [{ "path": "/api/billing/reconcile", "schedule": "0 * * * *" }],
  "git": {
    "deploymentEnabled": false
  }
}
