// The launch demo: a Slack ticket lands → Ship picks it up → Codex builds with
// your conventions → a PR opens for review. Record it, host it, and set this
// URL — ShipDemo.tsx falls back to a placeholder until it's set.
export const SHIP_DEMO_VIDEO_URL = "";
