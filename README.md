# PR screenshots — workflow Slack avatar upload

UX screenshots referenced from the pull request body. This branch holds images
only; it is not part of the product and never merges to `main`. Delete it once
the PR is closed and the images are no longer needed.
